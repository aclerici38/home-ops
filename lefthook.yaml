---
remotes:
  - git_url: "https://github.com/home-operations/.github"
    ref: "main"
    configs:
      - "lefthook.common.toml"
pre-commit:
  parallel: true
  commands:
    nixfmt:
      glob: "nix-config/**/*.nix"
      run: nixfmt --check {staged_files}
      stage_fixed: true
