---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: ${APP}-values-path
configMapGenerator:
  - name: ${APP}-values-path
    literals:
      - path: "${PATH}"
generatorOptions:
  disableNameSuffixHash: true
components:
  ../../components/helm
