---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: ${APP}-values-path
    literals:
      - path: "${PATH}"
generatorOptions:
  disableNameSuffixHash: true
components:
  ../../components/helm
