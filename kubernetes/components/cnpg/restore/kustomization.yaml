---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./cluster.yaml
  - ../common
patches:
  - path: ../base/cluster.yaml
    target:
      kind: Cluster
  - path: ../common/plugins.yaml
    target:
      kind: Cluster
