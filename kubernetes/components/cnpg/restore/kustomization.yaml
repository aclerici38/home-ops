---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
components:
  # Order matters otherwise the patch in common won't apply to the base
  - ../base
  - ../common
patches:
  - path: ./recovery.yaml
    target:
      kind: Cluster
