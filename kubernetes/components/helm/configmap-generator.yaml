---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: "test-configmap-generator"
configMapGenerator:
  - name: "test-helm-values"
    files:
      - ./values.yaml
# configurations:
#   - kustomizeconfig.yaml
