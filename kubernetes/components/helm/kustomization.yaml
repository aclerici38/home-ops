---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
vars:
  - name: PATH
    objref:
      name: ${APP}-values-path
      kind: ConfigMap
    fieldref:
      fieldpath: data.path
resources:
  - ./configmap-generator.yaml
