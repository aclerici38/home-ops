apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: ${APP}-timeout
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: ${APP}
  timeout:
    http:
      requestReceivedTimeout: ${ENVOY_TIMEOUT:-1m}
