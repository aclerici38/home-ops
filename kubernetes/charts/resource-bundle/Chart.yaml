---
# yaml-language-server: $schema=https://json.schemastore.org/chart.json
apiVersion: v2
name: resources
description: An app's supporting resources (db, cache, secrets, oidc, backups) in one release
type: application
version: 0.1.0
