---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: house-apps
  namespace: flux-system
spec:
  interval: 30m
  timeout: 10m
  dependsOn:
    - name: cloud-apps
  path: ./kubernetes/apps/house
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  kubeConfig:
    secretRef:
      name: house-cluster-kubeconfig
  postBuild:
    substitute:
      CLUSTER_NAME: house
