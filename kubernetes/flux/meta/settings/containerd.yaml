apiVersion: talos.dev/v1alpha1
kind: Configuration
metadata:
  name: containerd-registry-config
spec:
  machine:
    files:
      - content: |
          [plugins."io.containerd.grpc.v1.cri".registry.configs."ghcr.io".auth]
            username = "ant385525"
            password = "${GITHUB_PAT}"
        path: /etc/containerd/conf.d/ghcr.conf
        op: create
        permissions: 0o644
