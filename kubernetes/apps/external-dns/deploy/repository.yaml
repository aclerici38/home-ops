---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: external-dns
  namespace: flux-system
  labels:
    app.kubernetes.io/name: external-dns
spec:
  interval: 1h
  url: oci://ghcr.io/aclerici38/home-ops/external-dns
  ref:
    tag: main
  layerSelector:
    mediaType: application/vnd.cncf.flux.content.v1.tar+gzip
    operation: extract
