---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-dns-endpoints
  namespace: flux-system
spec:
  targetNamespace: external-dns
  dependsOn:
    - name: external-dns
  sourceRef:
    kind: OCIRepository
    name: external-dns
  path: ./endpoints
  commonMetadata:
    labels:
      app.kubernetes.io/name: external-dns-endpoints
