---
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: anthony
spec:
  store: ceph-objectstore
  keys:
    - accessKeyRef:
        name: rook-ceph-secret
        key: accessKey
      secretKeyRef:
        name: rook-ceph-secret
        key: secretKey
