---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencloud-cache
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 25Gi
  storageClassName: ceph-filesystem
