---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: opencloud-cache
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Gi
