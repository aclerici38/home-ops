apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: channels-dvr-cache
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteMany
