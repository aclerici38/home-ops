apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: channels-dvr-recordings
spec:
  storageClassName: synology-nfs
  resources:
    requests:
      storage: 2Ti
  accessModes:
    - ReadWriteMany
