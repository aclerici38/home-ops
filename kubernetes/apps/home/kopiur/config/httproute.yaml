---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/gateway-api/1.6.2/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kopia-versity
spec:
  parentRefs:
    - name: internal
      kind: Gateway
      namespace: envoy-gateway
      sectionName: https
  hostnames:
    - kopia-versity.clerici.tech
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: versity-kopia-ui
          port: 51515
---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/gateway-api/1.6.2/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kopia-backblaze
spec:
  parentRefs:
    - name: internal
      kind: Gateway
      namespace: envoy-gateway
      sectionName: https
  hostnames:
    - kopia-backblaze.clerici.tech
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backblaze-kopia-ui
          port: 51515
