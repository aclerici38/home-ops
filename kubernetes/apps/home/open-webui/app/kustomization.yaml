---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./helmrelease.yaml
patchesJson6902:
  - target:
      group: postgresql.cnpg.io
      version: v1
      kind: Cluster
      name: ${APP}-db
    path: load-vector.yaml
