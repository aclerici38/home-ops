---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-cache
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteMany
