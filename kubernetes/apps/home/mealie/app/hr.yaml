---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/flux/2.9.5/helmrelease_v2.json?values=app-template/5.1.0
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mealie
spec:
  interval: 1h
  maxHistory: 1
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    timeout: 2m
    remediation:
      retries: 1
  upgrade:
    timeout: 2m
    remediation:
      retries: 1
  values:
    controllers:
      mealie:
        strategy: RollingUpdate
        pod:
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile: {type: RuntimeDefault}
        containers:
          mealie:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.25.0@sha256:a27f0a07d516bf982274cf832fd6cc89793700a7d0701f7d9d4fc1f3ab040a34
            env:
              BASE_URL: https://mealie.clerici.tech
              DAILY_SCHEDULE_TIME: 05:00
              ALLOW_SIGNUP: "false"
              LOG_LEVEL: debug
              ALLOW_PASSWORD_LOGIN: false
              SCRAPER_FLARESOLVERR_URL: http://flaresolverr.flaresolverr:8191
              # OIDC
              OIDC_AUTH_ENABLED: "true"
              OIDC_SIGNUP_ENABLED: "true"
              OIDC_PROVIDER_NAME: Pocket ID
              OIDC_REMEMBER_ME: "true"
              OIDC_USER_CLAIM: email
              OIDC_AUTO_REDIRECT: 'true'
              OIDC_CONFIGURATION_URL:
                secretKeyRef:
                  name: mealie-oidc-credentials
                  key: discovery_url
              OIDC_CLIENT_ID:
                secretKeyRef:
                  name: mealie-oidc-credentials
                  key: client_id
              OIDC_CLIENT_SECRET:
                secretKeyRef:
                  name: mealie-oidc-credentials
                  key: client_secret
              OPENAI_MODEL: gpt-5.2
              OPENAI_BASE_URL: https://llm.clerici.tech/v1
              # pocket-id access token, not an OpenAI key.
              OPENAI_API_KEY:
                secretKeyRef:
                  name: mealie-llm-token
                  key: LLM_GATEWAY_TOKEN
              # postgres
              DB_ENGINE: postgres
              POSTGRES_URL_OVERRIDE:
                valueFrom:
                  secretKeyRef:
                    name: mealie-postgres
                    key: uri
              # SMTP
              SMTP_HOST: smtp-relay.smtp-relay.svc.cluster.local
              SMTP_PORT: 25
              SMTP_AUTH_STRATEGY: None
              SMTP_FROM_EMAIL: bot@clerici.tech
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 100m
                memory: 100Mi
              limits:
                memory: 2Gi
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    port: &port 9000
                    path: /api/app/about
                  periodSeconds: 10
                  failureThreshold: 12
                  initialDelaySeconds: 10
                  timeoutSeconds: 1
              readiness: *probes
    service:
      mealie:
        controller: mealie
        ports:
          http:
            port: *port
    route:
      mealie:
        hostnames:
          - mealie.clerici.tech
        parentRefs:
          - name: external
            namespace: envoy-gateway
    persistence:
      data:
        existingClaim: mealie
        globalMounts:
          - path: /app/data
            subPath: data
          - path: /nltk_data
            subPath: nltk
      # Needed for recipe parser
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /tmp
            subPath: tmp
          - path: /nonexistent
            subPath: nonexistent
  dependsOn:
    - name: mealie-resources
