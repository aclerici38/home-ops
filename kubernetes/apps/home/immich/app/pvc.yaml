---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-ml-cache
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 50Gi
  accessModes:
    - ReadWriteOnce
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-thumbnails
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteOnce
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-encoded-video
spec:
  storageClassName: bonds-nfs
  resources:
    requests:
      storage: 500Gi
  accessModes:
    - ReadWriteOnce
