---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/flux/2.8.6/helmrelease_v2.json?values=app-template/4.6.2
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: samba
spec:
  interval: 1h
  maxHistory: 1
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    timeout: 2m
    remediation:
      retries: 1
  upgrade:
    timeout: 2m
    remediation:
      retries: 1
  values:
    controllers:
      samba:
        strategy: RollingUpdate
        pod:
          securityContext:
            runAsNonRoot: false
            runAsGroup: 0
            runAsUser: 0
            seccompProfile:
              type: RuntimeDefault
        containers:
          main:
            image:
              repository: ghcr.io/crazy-max/samba
              tag: 4.22.8@sha256:3a518898ba29d719168af9d96ef11f8388d1a87db79d4ac8fade74e86c3f7fd6
            envFrom:
              - secretRef:
                  name: samba
            resources:
              requests:
                cpu: 500m
                memory: 20Mi
              limits:
                memory: 1Gi
            securityContext:
              allowPrivilegeEscalation: false
    service:
      main:
        controller: samba
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: "${SAMBA_LB_IP:-1.1.1.1}"
          coredns.io/hostname: "${APP_URL}"
        ports:
          smb:
            port: 445
            protocol: TCP
    persistence:
      config:
        type: configMap
        name: samba
        globalMounts:
          - path: /data/config.yml
            subPath: config.yml
      time-machine:
        existingClaim: time-machine
        globalMounts:
          - path: /samba/time-machine
    configMaps:
      config:
        data:
          config.yml: |
            auth:
              - user: time_machine_users
                group: time_machine_users
                uid: 1000
                gid: 1000
                password: $${SMB_PASS}
            
            global:
              - "force user = root"
              - "force group = root"
              - "ea support = yes"
              - "vfs objects = catia fruit streams_xattr"
              - "fruit:aapl = yes"
              - "fruit:metadata = stream"
              - "fruit:model = MacSamba"
              - "fruit:veto_appledouble = no"
              - "fruit:nfs_aces = no"
              - "fruit:posix_rename = yes"
              - "fruit:zero_file_id = yes"
              - "fruit:wipe_intentionally_left_blank_rfork = yes"
              - "fruit:delete_empty_adfiles = yes"
              - "fruit:time machine = yes"
              - "spotlight = no"
              - "server min protocol = SMB2"
              - "server max protocol = SMB3"
              # Allow use of LB IP
              - "host msdfs = no"
            
            share:
              - name: time_machine
                path: /samba/time-machine
                browsable: yes
                readonly: no
                guestok: no
                validusers: time_machine_users
                writelist: time_machine_users
