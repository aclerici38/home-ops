---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.35.2-standalone-strict/persistentvolume-v1.json
apiVersion: v1
kind: PersistentVolume
metadata:
  name: time-machine-pv
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: "59992103190528"
  claimRef:
    apiVersion: v1
    kind: PersistentVolumeClaim
    name: time-machine
    namespace: ${NS}
  csi:
    driver: zfs.csi.openebs.io
    fsType: zfs
    volumeAttributes:
      openebs.io/poolname: tank
    volumeHandle: time-machine
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - bonds
  persistentVolumeReclaimPolicy: Retain
  storageClassName: zfs-tank
  volumeMode: Filesystem
