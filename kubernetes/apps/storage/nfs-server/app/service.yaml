apiVersion: v1
kind: Service
metadata:
  name: nfs-server
  annotations:
    io.cilium/lb-ipam-ips: "10.38.55.50"
spec:
  selector:
    app: nfs-server
  type: LoadBalancer
  ports:
    - name: nfs
      port: 2049
      targetPort: 2049
      protocol: TCP
