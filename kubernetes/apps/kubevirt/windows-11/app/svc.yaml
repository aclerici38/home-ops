---
apiVersion: v1
kind: Service
metadata:
  name: win11-rdp
  annotations:
    coredns.io/hostname: windows.${SECRET_DOMAIN}
    lbipam.cilium.io/ips: 10.38.55.8
spec:
  type: LoadBalancer
  selector:
    vm.kubevirt.io/name: windows-11
  ports:
    - protocol: TCP
      port: 3389
      targetPort: 3389
