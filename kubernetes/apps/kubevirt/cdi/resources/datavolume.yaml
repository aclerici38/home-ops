---
apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: images
spec:
  source:
    upload: {}
  pvc:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 20Gi
    storageClassName: ceph-block
