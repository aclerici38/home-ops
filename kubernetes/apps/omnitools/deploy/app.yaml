---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: omnitools
  namespace: flux-system
spec:
  targetNamespace: omnitools
  sourceRef:
    kind: OCIRepository
    name: omnitools
  path: ./app
  commonMetadata:
    labels:
      app.kubernetes.io/name: omnitools
