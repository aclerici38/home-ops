---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: matter-server
  namespace: flux-system
  labels:
    app.kubernetes.io/name: matter-server
spec:
  interval: 1h
  url: oci://ghcr.io/aclerici38/home-ops/matter-server
  ref:
    tag: main
  layerSelector:
    mediaType: application/vnd.cncf.flux.content.v1.tar+gzip
    operation: extract
