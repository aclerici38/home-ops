---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: APP-app-vars
    literals:
      - APP=seer
components:
  - ../../../components/ks/app
  - ../../../components/kustomize/cnpg/restore
  - ../../../components/kustomize/volsync/versity
