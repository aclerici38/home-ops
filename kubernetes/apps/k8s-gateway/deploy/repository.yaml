---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: k8s-gateway
  namespace: flux-system
  labels:
    app.kubernetes.io/name: k8s-gateway
spec:
  interval: 1h
  url: oci://ghcr.io/aclerici38/home-ops/k8s-gateway
  ref:
    tag: main
  layerSelector:
    mediaType: application/vnd.cncf.flux.content.v1.tar+gzip
    operation: extract
