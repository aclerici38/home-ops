---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/cert-manager/1.20.0/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${SECRET_DOMAIN/./-}
spec:
  dnsNames:
    - "${SECRET_DOMAIN}"
    - "*.${SECRET_DOMAIN}"
  duration: 160h
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
  secretName: ${SECRET_DOMAIN/./-}-tls
  usages:
    - digital signature
    