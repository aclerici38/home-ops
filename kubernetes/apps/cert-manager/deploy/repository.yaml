---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: cert-manager
  namespace: flux-system
  labels:
    app.kubernetes.io/name: cert-manager
spec:
  interval: 1h
  url: oci://ghcr.io/aclerici38/home-ops/cert-manager
  ref:
    tag: main
  layerSelector:
    mediaType: application/vnd.cncf.flux.content.v1.tar+gzip
    operation: extract
