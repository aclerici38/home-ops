apiVersion: gateway.networking.k8s.io/v1alpha3
kind: BackendTLSPolicy
metadata:
  name: omada-tls-policy
spec:
  targetRefs:
    - kind: Service
      name: omada-controller
  validation:
    wellKnownCACertificates: System
    hostname: "omada.${CLUSTER_DOMAIN}"
