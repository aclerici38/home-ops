---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omada-controller-logs
spec:
  storageClassName: zfs-nvme
  resources:
    requests:
      storage: 2Gi
  accessModes:
    - ReadWriteMany
