apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: coraza
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: external
  wasm:
    - name: coraza
      rootID: coraza-root
      code:
        type: Image
        image:
          url: ghcr.io/corazawaf/coraza-proxy-wasm:0.5.0
      config:
        default_directives: "default"
        directives_map:
          default:
            - "SecDebugLogLevel 9"
            - "SecRuleEngine On"
            - "Include @crs-setup-conf"
            - "Include @owasp_crs/*.conf"
            # Pocket id
            - >
              SecRule REQUEST_URI "@beginsWith /api/oidc/clients"
              "id:1000001,phase:1,pass,nolog,ctl:ruleRemoveById=949111"
