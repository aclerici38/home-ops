---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: backups
spec:
  storageClassName: zfs-ssd
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "966367641600"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: immich
  namespace: storage
spec:
  storageClassName: zfs-tank
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 43.6Ti
