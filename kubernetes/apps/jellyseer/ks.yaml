---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app jellyseer
  namespace: flux-system
spec:
  targetNamespace: *app
  dependsOn:
    - name: cloudnative-pg
    - name: volsync
    - name: versity
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  path: ./kubernetes/apps/jellyseer/app
  components:
    - ../../../components/namespace
    - ../../../components/cnpg/helm/restore
    - ../../../components/volsync/helm/versity
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: true
  interval: 30m
  timeout: 3m
  # patches:
  #   - patch: |-
  #       apiVersion: postgresql.cnpg.io/v1
  #       kind: Cluster
  #       metadata:
  #         name: patch
  #       spec:
  #         bootstrap:
  #           recovery:
  #             recoveryTarget:
  #               targetTime: "2025-09-04T03:50:00-07:00"
  #               exclusive: true
  #     target:
  #       kind: Cluster
