---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: patch
spec:
  dependsOn:
    - name: external-secrets-stores
      namespace: cluster-infra
  postBuild:
    substituteFrom:
      - kind: Secret
        name: immich
