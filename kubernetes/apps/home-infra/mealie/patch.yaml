---
# yaml-language-server: $schema=https://k8s-versioned-schemas.pages.dev/flux/2.8.1/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mealie
spec:
  patches:
    - patch: |
        apiVersion: postgresql.cnpg.io/v1
        kind: Cluster
        metadata:
          name: patch
        spec:
          bootstrap:
            recovery:
              recoveryTarget:
                targetTLI: "112"
                targetLSN: "2/E8000130"
      target:
        kind: Cluster
