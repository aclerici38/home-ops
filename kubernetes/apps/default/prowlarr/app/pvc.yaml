apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-metadata
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteOnce
