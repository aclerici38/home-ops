apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vuetorrent-config
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 2Gi
  accessModes:
    - ReadWriteOnce
