apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-library
spec:
  storageClassName: truenas-iscsi
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteMany
