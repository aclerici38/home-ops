---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app fileflows
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  dependsOn:
    - name: nvidia-device-plugin
      namespace: kube-system
  install:
    timeout: 5m
    remediation:
      retries: 1
  upgrade:
    timeout: 2m
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 1
  values:
    controllers:
      server:
        pod:
          hostname: ff-server
        containers:
          app:
            image:
              repository: ghcr.io/ant385525/fileflows-server
              tag: 25.2.3.4462@sha256:4ff0c3cd6e30d8566fc6bdf0cf7876d35ea229d1e3a5cf2b6a98a1aeebf3f231
            env:
              TZ: "${TZ}"
              SQLITE_TMPDIR: &sqliteTmp /tmp/sqlite/
            resources: &resources
              requests:
                cpu: 100m
                memory: 350M
              limits:
                memory: 6Gi
                nvidia.com/gpu: 1
            securityContext: &secOps
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop:
                  - ALL
      node:
        pod:
          hostname: ff-local-node
        containers:
          app:
            image:
              repository: ghcr.io/ant385525/fileflows-node
              tag: 25.2.3.4462@sha256:8c584ae51ee3dcaeffe4498ac95c27e67963b7af3fec995df0dacc87b9f16a70
            env:
              TZ: "${TZ}"
              ServerUrl: http://fileflows:5000
            resources: *resources
            securityContext: *secOps
    defaultPodOptions:
      runtimeClassName: nvidia
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: Always
        seccompProfile: { type: RuntimeDefault }
      nodeSelector:
        kubernetes.io/hostname: "bonds"
    service:
      *app :
        controller: server
        ports:
          http:
            port: 5000
    ingress:
      *app :
        className: internal
        hosts:
          - host: "fileflows.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: *app
                  port: http
    persistence:
      config:
        existingClaim: *app
        advancedMounts:
          server:
            app:
              - path: /app/Data
      node-data:
        type: emptyDir
        advancedMounts:
          node:
            app:
              - path: /app/Data
      server-logs:
        enabled: true
        existingClaim: fileflows-logs
        advancedMounts:
          server:
            app:
              - path: /app/Logs
      node-logs:
        enabled: true
        type: emptyDir
        sizeLimit: 1Gi
        advancedMounts:
          node:
            app:
              - path: /app/Logs
      temp:
        enabled: true
        type: emptyDir
        globalMounts:
          - path: /temp
      server-common:
        existingClaim: fileflows-server-common
        advancedMounts:
          server:
            app:
              - path: /app/common
      node-common:
        existingClaim: fileflows-node-common
        advancedMounts:
          node:
            app:
              - path: /app/common
      dockermods:
        type: emptyDir
        globalMounts:
          - path: /app/DockerMods
      manually-added:
        type: emptyDir
        globalMounts:
          - path: /app/ManuallyAdded
      sqlite-tmp:
        type: emptyDir
        medium: Memory
        globalMounts:
          - path: *sqliteTmp
      calibre-library:
        existingClaim: calibre-library
        globalMounts:
          - path: /mnt/calibre-library
      ebook2audio-output:
        existingClaim: ebook2audiobook-output
        globalMounts:
          - path: /mnt/ebook2audio/output
      ebook2audio-ingest:
        existingClaim: ebook2audiobook-ingest
        globalMounts:
          - path: mnt/ebook2audio/ingest
      media:
        enabled: true
        type: nfs
        server: 192.168.9.10
        path: /mnt/tank/media
        globalMounts:
          - path: /allMedia
