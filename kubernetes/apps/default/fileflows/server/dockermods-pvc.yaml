apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-common
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 25Gi
  accessModes:
    - ReadWriteMany
