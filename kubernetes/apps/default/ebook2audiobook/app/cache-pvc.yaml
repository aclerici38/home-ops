apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebook2audiobook-cache
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 50Gi
  accessModes:
    - ReadWriteOnce
