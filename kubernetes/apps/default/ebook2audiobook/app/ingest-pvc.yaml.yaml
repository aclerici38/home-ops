apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebook2audiobook-ingest
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
