apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-cache
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 20Gi
  accessModes:
    - ReadWriteMany
