---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: octoprint
  namespace: flux-system
spec:
  targetNamespace: octoprint
  sourceRef:
    kind: OCIRepository
    name: octoprint
  path: ./app
  commonMetadata:
    labels:
      app.kubernetes.io/name: octoprint
