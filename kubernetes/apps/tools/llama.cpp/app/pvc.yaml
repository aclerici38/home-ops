---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-model-cache
spec:
  storageClassName: nfs-ssd
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteMany
