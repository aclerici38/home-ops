apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-library
spec:
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteMany
