bgpControlPlane:
  enabled: true
  secretsNamespace:
    *app
cluster:
  name: "${CLUSTER_NAME}"
  id: "${CLUSTER_ID}"
ipv4NativeRoutingCIDR: "${POD_NET}"
cni:
  exclusive: false
clustermesh:
  apiserver:
    service:
      nodePort: ${CLUSTERMESH_PORT}
