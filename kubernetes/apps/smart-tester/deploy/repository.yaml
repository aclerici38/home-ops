---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: smart-tester
  namespace: flux-system
  labels:
    app.kubernetes.io/name: smart-tester
spec:
  interval: 1h
  url: oci://ghcr.io/aclerici38/home-ops/smart-tester
  ref:
    tag: main
  layerSelector:
    mediaType: application/vnd.cncf.flux.content.v1.tar+gzip
    operation: extract
