---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: cilium-helm-values
    files:
      - ./base-values.yaml
configurations:
  - ../../../templates/helm/kustomizeconfig.yaml
