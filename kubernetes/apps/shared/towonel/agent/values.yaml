---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.36.4-standalone-strict/configmap-v1.json
apiVersion: v1
kind: ConfigMap
metadata:
  name: towonel-agent-values
  namespace: towonel
data:
  values.yaml: |-
    controllers:
      main:
        replicas: 1
    defaultPodOptions:
      hostUsers: false
    agent:
      inviteTokenSecret:
        name: ${CLUSTER_NAME}-k8s-token
        key: token
      services:
        - hostname: "*.${EXTERNAL_DOMAIN}"
          origin: external.envoy-gateway.svc.cluster.local:443
        - hostname: "*.${CLUSTER_NAME}.clerici.tech"
          origin: external.envoy-gateway.svc.cluster.local:443
    serviceMonitor:
      enabled: true
    monitoring:
      prometheusRule:
        enabled: true
      dashboards:
        enabled: true
        grafanaOperator:
          enabled: true
          matchLabels:
            grafana.internal/instance: grafana
