---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: ${APP}-helm-values
    files:
      - ./values.yaml
configurations:
  - ../../../../templates/kustomizeconfig.yaml
