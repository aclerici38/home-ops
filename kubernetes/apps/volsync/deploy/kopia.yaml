---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: volsync-kopia
  namespace: flux-system
spec:
  targetNamespace: volsync
  dependsOn:
    - name: volsync
  sourceRef:
    kind: OCIRepository
    name: volsync
  path: ./kopia
  commonMetadata:
    labels:
      app.kubernetes.io/name: volsync-kopia
