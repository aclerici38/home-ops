---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: smartctl-exporter
  namespace: flux-system
spec:
  targetNamespace: smartctl-exporter
  sourceRef:
    kind: OCIRepository
    name: smartctl-exporter
  path: ./app
  commonMetadata:
    labels:
      app.kubernetes.io/name: smartctl-exporter
