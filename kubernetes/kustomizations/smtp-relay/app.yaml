---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: smtp-relay
  namespace: flux-system
spec:
  targetNamespace: smtp-relay
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/apps/smtp-relay/app
  commonMetadata:
    labels:
      app.kubernetes.io/name: smtp-relay
