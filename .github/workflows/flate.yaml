---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Flate

on:
  pull_request:
    branches:
      - main
    paths:
      - kubernetes/**
      - .github/workflows/flate.yaml
  push:
    branches:
      - main
    paths:
      - kubernetes/**
      - .github/workflows/flate.yaml

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

env:
  # renovate: datasource=docker depName=ghcr.io/siderolabs/kubelet
  KUBE_VERSION: v1.36.1
  API_VERSIONS: cert-manager.io/v1,monitoring.coreos.com/v1,monitoring.coreos.com/v1alpha1,gateway.networking.k8s.io/v1,gateway.networking.k8s.io/v1beta1,grafana.integreatly.org/v1beta1,external-secrets.io/v1,volsync.backube/v1alpha1,snapshot.storage.k8s.io/v1

jobs:
  test:
    name: Flate Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Pull Request Branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: pull
          persist-credentials: false

      - name: Setup flate
        uses: home-operations/flate/action@62f56d7f2a9a7ade2d35a0fed89bde3ff79fdacd # 0.1.26
        with:
          cache: true

      - name: Run flate test all
        run: |-
          flate test all \
            --allow-missing-secrets=true \
            --kube-version "${KUBE_VERSION}" \
            --api-versions "${API_VERSIONS}" \
            --skip-crds=false \
            --skip-secrets=false \
            --path ./pull

  diff:
    if: ${{ github.event_name == 'pull_request' }}
    name: Flate Diff (${{ matrix.resource }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        resource:
          - helmrelease
          - kustomization
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          path: pull
          persist-credentials: false

      - name: Setup flate
        uses: home-operations/flate/action@62f56d7f2a9a7ade2d35a0fed89bde3ff79fdacd # 0.1.26
        with:
          cache: true

      - name: Run flate diff
        id: diff
        env:
          BASE_REF: ${{ github.base_ref }}
          IS_UPGRADE: ${{ contains(github.event.pull_request.labels.*.name, 'upgrade') }}
        run: |-
          args=(
            --allow-missing-secrets=true
            --kube-version "${KUBE_VERSION}"
            --api-versions "${API_VERSIONS}"
            --skip-crds=false
            --skip-secrets=false
            --base "origin/${BASE_REF}"
            --path ./pull
          )
          if [[ "${IS_UPGRADE}" == "true" ]]; then
            args+=(--is-upgrade)
          fi
          {
            echo 'diff<<EOF'
            flate diff ${{ matrix.resource }} "${args[@]}" || true
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - if: ${{ steps.diff.outputs.diff != '' }}
        name: Generate Token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          client-id: ${{ secrets.BOT_APP_CLIENT_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          permission-pull-requests: write

      - if: ${{ steps.diff.outputs.diff != '' }}
        name: Add Comment
        uses: marocchino/sticky-pull-request-comment@0ea0beb66eb9baf113663a64ec522f60e49231c0 # v3.0.4
        with:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          header: ${{ github.event.pull_request.number }}/kubernetes/${{ matrix.resource }}
          message: |-
            ```diff
            ${{ steps.diff.outputs.diff }}
            ```

  images-list:
    if: ${{ github.event_name == 'pull_request' }}
    name: Flate Images Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      images: ${{ steps.list.outputs.images }}
    steps:
      - name: Checkout Pull Request Branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          path: pull
          persist-credentials: false

      - name: Setup flate
        uses: home-operations/flate/action@62f56d7f2a9a7ade2d35a0fed89bde3ff79fdacd # 0.1.26
        with:
          cache: true

      - name: List changed images
        id: list
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |-
          images=$(flate get images \
            --allow-missing-secrets=true \
            --kube-version "${KUBE_VERSION}" \
            --api-versions "${API_VERSIONS}" \
            --base "origin/${BASE_REF}" \
            --path ./pull \
            -o name | sort -u | jq -R . | jq -sc . || echo '[]')
          echo "images=${images}" >> "$GITHUB_OUTPUT"
          echo "${images}" | jq .

  images-inspect:
    if: ${{ needs.images-list.outputs.images != '[]' }}
    name: Flate Images - Inspect (${{ matrix.image }})
    needs: images-list
    runs-on: ubuntu-latest
    permissions: {}
    strategy:
      matrix:
        image: ${{ fromJson(needs.images-list.outputs.images) }}
      fail-fast: false
    steps:
      - name: Inspect image
        env:
          IMAGE: ${{ matrix.image }}
        run: docker buildx imagetools inspect "${IMAGE}"

  success:
    if: ${{ !cancelled() && github.event_name == 'pull_request' }}
    needs:
      - test
      - diff
      - images-list
      - images-inspect
    name: Flate - Success
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Any jobs failed?
        if: ${{ contains(needs.*.result, 'failure') }}
        run: exit 1

      - name: All jobs passed or skipped?
        if: ${{ !(contains(needs.*.result, 'failure')) }}
        run: echo "All jobs passed or skipped" && echo "${{ toJSON(needs.*.result) }}"
