---
machine:
  kubelet:
    extraArgs:
      max-pods: 500
      allowed-unsafe-sysctls: net.ipv6.conf.*
    nodeIP:
      validSubnets:
        - "${NODE_V4_SUBNET}"
        - "${NODE_V6_SUBNET}"
    extraConfig:
      featureGates:
        ImageVolume: true
