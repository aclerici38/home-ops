---
machine:
  kubelet:
    extraArgs:
      max-pods: 500
      allowed-unsafe-sysctls: net.ipv6.conf.all.forwarding
    nodeIP:
      validSubnets:
        - "${NODE_SUBNET}"
    extraConfig:
      featureGates:
        ImageVolume: true
