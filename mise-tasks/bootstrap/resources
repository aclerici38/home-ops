#!/usr/bin/env bash
#MISE description="Bootstrap namespaces + secrets for a Talos cluster"
#USAGE arg "<cluster>" help="which Talos cluster" {
#USAGE   choices "home" "cloud"
#USAGE }
set -euo pipefail
source "${MISE_PROJECT_ROOT}/mise-tasks/lib/cluster.sh"
select_cluster "$usage_cluster"
mise run bootstrap:secrets "$KUBE_CONTEXT" "flux-system onepassword-connect kgateway"
