#!/usr/bin/env bash
#MISE description="Full bootstrap (namespaces + secrets, then helmfile)"
#USAGE arg "<cluster>" help="which Talos cluster" {
#USAGE   choices "home" "cloud"
#USAGE }
set -euo pipefail
mise run bootstrap:resources "$usage_cluster"
mise run bootstrap:helmfile "$usage_cluster"
